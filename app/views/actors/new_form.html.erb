<h1>Add a new Actor</h1>

<!-- This form gets sent to the following URL, which is linked, via routes.rb, to the
'create row' action in actors_controller.rb. This, in turn, creates a new row
in the Actors table and adds the values from the input name fields below (both to the)
Table and to the query string in the URL for 'add_actor'. It then renders 'create_row.html.erb'
because that is the default value for the 'create row' method, if not specified. -->

<form action="http://localhost:3000/add_actor">


  <div>
    <label for="name">Name</label>
    <input name="the_name" type="text" id="name" placeholder="Enter name here">
  </div>

  <div>
    <label for="dob">DOB</label>
    <input name="the_dob" type="text" id="dob" placeholder="Month Day, Year">
  </div>

  <div>
    <label for="bio">Biography</label>
    <textarea
      name="the_bio" type="text" id="bio" placeholder="Enter bio in this box" rows="10">
    </textarea>
  </div>

  <div>
    <label for="image_url">Picture URL</label>
    <input name="the_image_url" type="text" id="image_url" placeholder="Enter image url here">
  </div>

  <div>
    <input type="Submit" value="Create Actor"> or <a href="/actors">Cancel</a>
  </div>

</form>
